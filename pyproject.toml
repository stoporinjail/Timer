[tool.poetry]
name = "repl_python3_Timer"
version = "0.1.0"
description = ""
authors = ["stoporinjail <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
playsound = "^1.2.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
